{
  "name": "webtuios",
  "version": "0.0.1",
  "private": false,
  "type": "module",
  "scripts": {
    "dev": "node scripts/prepare-dev.mjs && vite",
    "build": "node scripts/prepare-build.mjs && node scripts/check-image.mjs && vite build",
    "preview": "vite preview",
    "image": "node scripts/prepare-build.mjs"
  },
  "dependencies": {
    "@leaningtech/cheerpx": "1.3.8",
    "@xterm/addon-fit": "0.11.0",
    "@xterm/xterm": "6.0.0",
    "@xterm/addon-webgl": "0.19.0"
  },
  "devDependencies": {
    "vite": "8.2.1"
  },
  "license": "MIT",
  "engines": {
    "node": ">=20.19.0"
  }
}
